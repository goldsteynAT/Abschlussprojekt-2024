version: '3.8'

services:
  backend:
    build: ./backend-server
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URL=mongodb+srv://danielessl:1a1o97XqzQV8Uz@abschlussprojekt.ddl6h.mongodb.net/?retryWrites=true&w=majority&appName=Abschlussprojekt
    depends_on:
      - mongo

  frontend:
    build: ./website
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:5000

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
